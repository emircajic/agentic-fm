/**
 * Response_GetWarnings ( response )
 *
 * PARAMETERS:
 *   @response (JSONObject) A Response envelope returned by a sub-script.
 *
 * RETURNS:
 *   (JSONArray) The warning message array from messages.warning.
 *   Each element: { code, text, field, context, script, timestamp }
 *
 * NOTES:
 *   Pure reader — does not touch $response.
 */

JSONGetElement ( response ; "messages.warning" )
